void main() {
  /**
   * ⭐️ var 혹은 dynamic을 통해 가변적으로 변하는 형태의 변수타입 지정이 가능하다.
   * */
  // var name;
  dynamic name;
  name = '다이나믹타입입니다.';
  print(name);

  /****/

  name = 12;
  print(name);

  /****/

  name = true;
  print(name);
}
